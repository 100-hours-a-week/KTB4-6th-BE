spring:
  application:
    name: meety
  profiles:
    active:
      - local

jwt:
  secret: ${JWT_SECRET}
  access-token-validity: 30m
  refresh-token-validity: 14d

management:
  endpoints:
    web:
      exposure:
        include: health
